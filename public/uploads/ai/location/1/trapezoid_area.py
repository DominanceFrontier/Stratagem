print "Area of a trapezoid.\n"

height = float(input("Enter the height of the trapezoid: "))
top_length = float(input("Enter the length of the top base: "))
bot_length = float(input("Enter the length of the bottom base: "))

print "The area of the trapezoid is: ", 0.5 * (top_length + bot_length) * height
